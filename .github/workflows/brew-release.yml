# Publish to Homebrew when a GitHub release is promoted from prerelease to full release.
# Updates Formula/tish.rb in this repo (monorepo tap). No separate homebrew-tish repo needed.
# Users: brew tap tishlang/tish https://github.com/tishlang/tish && brew install tish

name: Homebrew release

on:
  release:
    types: [published, edited]

jobs:
  publish:
    name: Publish to Homebrew (update formula in repo)
    if: github.event.release.prerelease == false
    runs-on: ubuntu-latest
    permissions:
      contents: write  # read release assets, push formula to repo, update release body
    steps:
      - name: Download release binaries and compute SHA256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        id: shas
        run: |
          TAG="${{ github.event.release.tag_name }}"
          VERSION="${TAG#v}"
          REPO="${{ github.repository }}"
          BASE="https://github.com/${REPO}/releases/download/${TAG}"
          ASSETS='${{ toJSON(github.event.release.assets) }}'

          get_asset_url() {
            echo "$ASSETS" | jq -r ".[] | select(.name == \"$1\") | .url"
          }
          download_and_sha() {
            local name="$1"
            local url
            url=$(get_asset_url "$name")
            if [ -z "$url" ] || [ "$url" = "null" ]; then
              echo "Release has no asset $name"
              exit 1
            fi
            curl -sL -H "Authorization: Bearer $GITHUB_TOKEN" -H "Accept: application/octet-stream" "$url" -o "$name"
            sha256sum "$name" | awk '{print $1}'
          }

          SHA_ARM=$(download_and_sha "tish-darwin-arm64")
          SHA_X64=$(download_and_sha "tish-darwin-x64")
          SHA_LINUX_X64=$(download_and_sha "tish-linux-x64")
          SHA_LINUX_ARM=$(download_and_sha "tish-linux-arm64")

          BG_SHA_ARM=$(download_and_sha "tish-bindgen-darwin-arm64")
          BG_SHA_X64=$(download_and_sha "tish-bindgen-darwin-x64")
          BG_SHA_LINUX_X64=$(download_and_sha "tish-bindgen-linux-x64")
          BG_SHA_LINUX_ARM=$(download_and_sha "tish-bindgen-linux-arm64")

          echo "sha_darwin_arm64=$SHA_ARM" >> $GITHUB_OUTPUT
          echo "sha_darwin_x64=$SHA_X64" >> $GITHUB_OUTPUT
          echo "sha_linux_x64=$SHA_LINUX_X64" >> $GITHUB_OUTPUT
          echo "sha_linux_arm64=$SHA_LINUX_ARM" >> $GITHUB_OUTPUT
          echo "bindgen_sha_darwin_arm64=$BG_SHA_ARM" >> $GITHUB_OUTPUT
          echo "bindgen_sha_darwin_x64=$BG_SHA_X64" >> $GITHUB_OUTPUT
          echo "bindgen_sha_linux_x64=$BG_SHA_LINUX_X64" >> $GITHUB_OUTPUT
          echo "bindgen_sha_linux_arm64=$BG_SHA_LINUX_ARM" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Checkout repo (main branch)
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate and push formula
        env:
          VERSION: ${{ steps.shas.outputs.version }}
          SHA_ARM: ${{ steps.shas.outputs.sha_darwin_arm64 }}
          SHA_X64: ${{ steps.shas.outputs.sha_darwin_x64 }}
          SHA_LINUX_X64: ${{ steps.shas.outputs.sha_linux_x64 }}
          SHA_LINUX_ARM: ${{ steps.shas.outputs.sha_linux_arm64 }}
          BG_SHA_ARM: ${{ steps.shas.outputs.bindgen_sha_darwin_arm64 }}
          BG_SHA_X64: ${{ steps.shas.outputs.bindgen_sha_darwin_x64 }}
          BG_SHA_LINUX_X64: ${{ steps.shas.outputs.bindgen_sha_linux_x64 }}
          BG_SHA_LINUX_ARM: ${{ steps.shas.outputs.bindgen_sha_linux_arm64 }}
          REPO: ${{ github.repository }}
        run: |
          TAG="v${VERSION}"
          BASE="https://github.com/${REPO}/releases/download/${TAG}"
          mkdir -p Formula
          cat > Formula/tish.rb << 'FORMULA'
          # typed: false
          # frozen_string_literal: true

          class Tish < Formula
            desc "Tish - minimal TS/JS-compatible language. Run, REPL, compile to native."
            homepage "https://github.com/tishlang/tish"
            version "VERSION_PLACEHOLDER"
            license "MIT"

            depends_on "tish-bindgen"

            on_macos do
              if Hardware::CPU.arm?
                url "BASE_PLACEHOLDER/tish-darwin-arm64"
                sha256 "SHA_ARM_PLACEHOLDER"

                def install
                  bin.install "tish-darwin-arm64" => "tish"
                end
              end
              if Hardware::CPU.intel?
                url "BASE_PLACEHOLDER/tish-darwin-x64"
                sha256 "SHA_X64_PLACEHOLDER"

                def install
                  bin.install "tish-darwin-x64" => "tish"
                end
              end
            end

            on_linux do
              if Hardware::CPU.arm?
                url "BASE_PLACEHOLDER/tish-linux-arm64"
                sha256 "SHA_LINUX_ARM_PLACEHOLDER"

                def install
                  bin.install "tish-linux-arm64" => "tish"
                end
              end
              if Hardware::CPU.intel?
                url "BASE_PLACEHOLDER/tish-linux-x64"
                sha256 "SHA_LINUX_X64_PLACEHOLDER"

                def install
                  bin.install "tish-linux-x64" => "tish"
                end
              end
            end

            test do
              assert_match(/^\d+\.\d+\.\d+/, shell_output("#{bin}/tish --version"))
            end
          end
          FORMULA
          sed -i.bak \
            -e "s|VERSION_PLACEHOLDER|${VERSION}|g" \
            -e "s|BASE_PLACEHOLDER|${BASE}|g" \
            -e "s|SHA_ARM_PLACEHOLDER|${SHA_ARM}|g" \
            -e "s|SHA_X64_PLACEHOLDER|${SHA_X64}|g" \
            -e "s|SHA_LINUX_X64_PLACEHOLDER|${SHA_LINUX_X64}|g" \
            -e "s|SHA_LINUX_ARM_PLACEHOLDER|${SHA_LINUX_ARM}|g" \
            Formula/tish.rb
          rm -f Formula/tish.rb.bak

          cat > Formula/tish-bindgen.rb << 'BINDGEN_FORMULA'
          # typed: false
          # frozen_string_literal: true

          class TishBindgen < Formula
            desc "CLI to generate Rust glue for Tish cargo: imports (tishlang-cargo-bindgen)"
            homepage "https://github.com/tishlang/tish"
            version "VERSION_PLACEHOLDER"
            license "MIT"

            on_macos do
              if Hardware::CPU.arm?
                url "BASE_PLACEHOLDER/tish-bindgen-darwin-arm64"
                sha256 "BG_SHA_ARM_PLACEHOLDER"

                def install
                  bin.install "tish-bindgen-darwin-arm64" => "tish-bindgen"
                end
              end
              if Hardware::CPU.intel?
                url "BASE_PLACEHOLDER/tish-bindgen-darwin-x64"
                sha256 "BG_SHA_X64_PLACEHOLDER"

                def install
                  bin.install "tish-bindgen-darwin-x64" => "tish-bindgen"
                end
              end
            end

            on_linux do
              if Hardware::CPU.arm?
                url "BASE_PLACEHOLDER/tish-bindgen-linux-arm64"
                sha256 "BG_SHA_LINUX_ARM_PLACEHOLDER"

                def install
                  bin.install "tish-bindgen-linux-arm64" => "tish-bindgen"
                end
              end
              if Hardware::CPU.intel?
                url "BASE_PLACEHOLDER/tish-bindgen-linux-x64"
                sha256 "BG_SHA_LINUX_X64_PLACEHOLDER"

                def install
                  bin.install "tish-bindgen-linux-x64" => "tish-bindgen"
                end
              end
            end

            test do
              assert_match(/tishlang-cargo-bindgen/, shell_output("#{bin}/tish-bindgen --help"))
            end
          end
          BINDGEN_FORMULA
          sed -i.bak \
            -e "s|VERSION_PLACEHOLDER|${VERSION}|g" \
            -e "s|BASE_PLACEHOLDER|${BASE}|g" \
            -e "s|BG_SHA_ARM_PLACEHOLDER|${BG_SHA_ARM}|g" \
            -e "s|BG_SHA_X64_PLACEHOLDER|${BG_SHA_X64}|g" \
            -e "s|BG_SHA_LINUX_X64_PLACEHOLDER|${BG_SHA_LINUX_X64}|g" \
            -e "s|BG_SHA_LINUX_ARM_PLACEHOLDER|${BG_SHA_LINUX_ARM}|g" \
            Formula/tish-bindgen.rb
          rm -f Formula/tish-bindgen.rb.bak

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/tish.rb Formula/tish-bindgen.rb
          if git diff --staged --quiet; then
            echo "No formula changes (version may already be published)"
          else
            git commit -m "tish ${VERSION}"
            git push
          fi

      - name: Update release description with Homebrew install
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          OWNER="${{ github.repository_owner }}"
          RELEASE_ID="${{ github.event.release.id }}"
          REPO="${{ github.repository }}"
          BREW_LINE="Homebrew: \`brew tap ${OWNER}/tish https://github.com/${OWNER}/tish && brew install tish\`"
          CURRENT_BODY=$(curl -s -H "Authorization: Bearer $GITHUB_TOKEN" \
            "https://api.github.com/repos/${REPO}/releases/${RELEASE_ID}" | jq -r '.body // ""')
          if [[ "$CURRENT_BODY" == *"brew install"* ]]; then
            echo "Release body already has Homebrew install"
          else
            NEW_BODY="${CURRENT_BODY}

          ---
          ${BREW_LINE}"
            curl -s -X PATCH \
              -H "Authorization: Bearer $GITHUB_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${REPO}/releases/${RELEASE_ID}" \
              -d "$(printf '%s' "$NEW_BODY" | jq -Rs '{body: .}')"
          fi
