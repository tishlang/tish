[package]
name = "tish_metal"
version = "0.1.0"
edition = "2021"
description = "Metal GPU compute backend for Tish — Apple Silicon only"

[target.'cfg(target_os = "macos")'.dependencies]
metal  = "0.29"
objc   = "0.2"
