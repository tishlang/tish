# Use this as the final `README.md` on `tishlang/tish-pg` before archiving

```markdown
# Moved to the Tish monorepo

**This repository is superseded.** Development continues in **`tishlang/tish`**:

| Was | Now |
|-----|-----|
| Rust crate `tish-pg` | [`crates/tish_pg`](https://github.com/tishlang/tish/tree/main/crates/tish_pg) (published as **`tishlang_pg`**) |
| npm `tish-pg` | **`@tishlang/pg`** ([`npm/pg`](https://github.com/tishlang/tish/tree/main/npm/pg)) |

Install from npm:

\`\`\`bash
npm install @tishlang/pg
\`\`\`

Docs: [PostgreSQL feature](https://tishlang.com/docs/features/pg/).

This repo may be **archived** read-only; open issues and PRs on **`tishlang/tish`**.
```
