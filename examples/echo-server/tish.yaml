name: echo-server

build:
  source: ./src/main.tish
  features:
    - http

deploy:
  replicas: 1
  strategy: rolling

networking:
  port: 3000
  protocol: http
  health_check:
    path: /health
    interval: 10s
    timeout: 5s
