name: mdx-docs

build:
  source: ./src/main.tish
  features:
    - http
    - fs

deploy:
  replicas: 1
  strategy: rolling

networking:
  port: 3000
  protocol: http
  health_check:
    path: /health
    interval: 10s
    timeout: 5s
