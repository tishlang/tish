{
  "name": "npm-usage",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "tish run src/main.tish",
    "compile": "tish compile src/main.tish -o app",
    "repl": "tish repl",
    "which": "which tish"

  },
  "dependencies": {
    "@tishlang/tish": "^1.0.0"
  },
  "tish": {
    "source": "./src/main.tish"
  }
}
