# Materialized by scripts/install-bin.js (postinstall / pre-pack)
bin/tish-lint

# Rust payload copied in by scripts/prepack.js and removed by scripts/postpack.js.
# Listed here so a failed pack leaves an obviously-untracked tree rather than a silently
# .git/info/exclude-hidden one.
/Cargo.toml
/crates/
/LICENSE
/justfile
