// MVP test: rest parameters
fun sum(...args) {
  any total = 0
  for (any x of args)
    total = total + x
  return total
}
print(sum(1, 2, 3))
print(sum(10, 20))

fun greet(first, ...rest) {
  return first + ":" + rest.length
}
print(greet("a", "b", "c"))
