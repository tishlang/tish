fn fib(n) { if (n < 2) { return n } return fib(n - 1) + fib(n - 2) }
fn work(n) { let s = 0; for (let i = 0; i < n; i++) { s = s + i } return s }
fn driver(n) { return work(n) + fib(10) }
console.log(String(driver(20)))
